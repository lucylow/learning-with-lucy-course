title: learning with lucy
description: 'machine learning '
template:
  repo: learning-with-lucy-template
  name: learning-with-lucy
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
